export const environment = {
  production: true,
  // Production: public API base URL only — no secrets here.
  // Updated during Azure deployment (Static Web Apps → App Service).
  apiBaseUrl: 'https://api.example.com',
};
